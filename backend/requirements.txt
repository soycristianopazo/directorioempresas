# ── Framework ────────────────────────────────────────────────────────────────
fastapi==0.110.3
uvicorn[standard]==0.25.0
watchfiles==0.21.0
python-multipart==0.0.22

# ── Base de datos ────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.46
alembic==1.18.0
asyncpg==0.31.0
# Solo para Alembic: el DDL con locks va por conexión síncrona en modo sesión.
psycopg2-binary==2.9.10

# ── Validación y configuración ───────────────────────────────────────────────
pydantic==2.12.0
pydantic-settings==2.7.0
email-validator==2.2.0

# ── Autenticación ────────────────────────────────────────────────────────────
PyJWT==2.11.0
# bcrypt fijado a 4.1.x: passlib 1.7.4 lee bcrypt.__about__.__version__, que
# desapareció en bcrypt 4.2. Con 4.2+ passlib emite un error al arrancar y,
# según la versión, deja de poder verificar hashes.
bcrypt==4.1.3
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# ── HTML público (perfiles indexables) ───────────────────────────────────────
jinja2==3.1.4

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==9.0.0
pytest-asyncio==0.24.0
anyio==4.12.0
httpx==0.28.1

# ── Tooling ──────────────────────────────────────────────────────────────────
black==24.10.0
flake8==7.1.1
mypy==1.13.0
isort==5.13.2
ruff==0.8.4
